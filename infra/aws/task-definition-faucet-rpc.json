{
  "cpu": "${TASK_CPU}",
  "memory": "${TASK_MEMORY}",
  "volumes": [],
  "networkMode": "awsvpc",
  "family": "${PRODUCT}-${APPLICATION}-${ENVIRONMENT}",
  "requiresCompatibilities": ["FARGATE"],
  "placementConstraints": [],
  "executionRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/${PRODUCT}-${APPLICATION}-ecs-taskexec-${AWS_REGION}-${ENVIRONMENT}",
  "taskRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/${PRODUCT}-${APPLICATION}-ecs-task-${AWS_REGION}-${ENVIRONMENT}",
  "containerDefinitions": [
    {
      "name": "${APPLICATION}",
      "cpu": "${TASK_CPU}",
      "memory": "${TASK_MEMORY}",
      "linuxParameters": {
        "initProcessEnabled": true
      },
      "essential": true,
      "volumesFrom": [],
      "mountPoints": [],
      "image": "",
      "portMappings": [
        {
          "protocol": "tcp",
          "containerPort": "${API_PORT}",
          "hostPort": "${API_PORT}"
        }
      ],
      "environment": [
        {
          "name": "AWS_REGION",
          "value": "${AWS_REGION}"
        },
        {
          "name": "ENVIRONMENT",
          "value": "${ENVIRONMENT}"
        },
        {
          "name": "POSTGRES_ENABLESSL",
          "value": "true"
        }
      ],
      "secrets": [
        {
          "name": "NUKLAI_RPC",
          "valueFrom": "arn:aws:ssm:${AWS_REGION}:${AWS_ACCOUNT_ID}:parameter/${ENVIRONMENT}/${PRODUCT}/${APPLICATION}/nuklaivm_rpc_url"
        },
        {
          "name": "PRIVATE_KEY_BYTES",
          "valueFrom": "arn:aws:ssm:${AWS_REGION}:${AWS_ACCOUNT_ID}:parameter/${ENVIRONMENT}/${PRODUCT}/${APPLICATION}/private_key_bytes"
        },
        {
          "name": "ADMIN_TOKEN",
          "valueFrom": "arn:aws:ssm:${AWS_REGION}:${AWS_ACCOUNT_ID}:parameter/${ENVIRONMENT}/${PRODUCT}/${APPLICATION}/admin_token"
        },
        {
          "name": "POSTGRES_HOST",
          "valueFrom": "arn:aws:ssm:${AWS_REGION}:${AWS_ACCOUNT_ID}:parameter/${ENVIRONMENT}/${PRODUCT}/shared/db_host"
        },
        {
          "name": "POSTGRES_PORT",
          "valueFrom": "arn:aws:ssm:${AWS_REGION}:${AWS_ACCOUNT_ID}:parameter/${ENVIRONMENT}/${PRODUCT}/shared/db_port"
        },
        {
          "name": "POSTGRES_USER",
          "valueFrom": "arn:aws:ssm:${AWS_REGION}:${AWS_ACCOUNT_ID}:parameter/${ENVIRONMENT}/${PRODUCT}/shared/db_username"
        },
        {
          "name": "POSTGRES_PASSWORD",
          "valueFrom": "arn:aws:ssm:${AWS_REGION}:${AWS_ACCOUNT_ID}:parameter/${ENVIRONMENT}/${PRODUCT}/shared/db_password"
        },
        {
          "name": "POSTGRES_DBNAME",
          "valueFrom": "arn:aws:ssm:${AWS_REGION}:${AWS_ACCOUNT_ID}:parameter/${ENVIRONMENT}/${PRODUCT}/shared/db_name"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/fargate/service/${PRODUCT}-${APPLICATION}-${ENVIRONMENT}",
          "awslogs-region": "${AWS_REGION}",
          "awslogs-stream-prefix": "ecs"
        }
      }
    }
  ]
}
